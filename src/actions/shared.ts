// import { getInitialData } from '../utils/api'
// // import { receiveFolders, getFolders } from './foldersActions'
// import { receiveMessages } from './messages'
// import { receiveFolderMessages } from './folderMessagesActions'

// export function handleInitialData() {
//   return (dispatch) => {
//     return getInitialData().then(({ folders, messages, folderMessages }) => {
//       // dispatch(receiveFolders(folders))
//       dispatch(getFolders())
//       dispatch(receiveMessages(messages))
//       dispatch(receiveFolderMessages(folderMessages))
//     })
//   }
// }
